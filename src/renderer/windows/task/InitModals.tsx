import { useCallback } from "react";
import { type TContent } from "@conceptions/Task";

export const TitleModal = () => {
  const onSuccess = useCallback((data: TContent) => {}, []);

  return <>{/* TitleModal editor will go here */}</>;
};
